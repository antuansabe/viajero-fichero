---
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import { Image } from "@astrojs/image/components";
import { authors } from "@data/authors";
import Pagetitle from "@components/pagetitle.astro";
import Link from "@components/ui/link.astro";
import { lazy } from "astro/zod";
---

<Layout title="About">
  <Container>
    <Pagetitle>
      <Fragment slot="title">Acerca de mi</Fragment>
      <Fragment slot="desc">Polifacético<br>💀</Fragment>
    </Pagetitle>

    <div class="grid md:grid-cols-1 place-items-center gap-10 mx-auto max-w-4xl mt-12">
      {
        authors.slice(0, 1).map((item) => (
          <div class="group">
            <div class="w-full aspect-square">
              <Image
                src={item.image}
                format="avif"
                width={480}
                height={480}
                alt="Team"
                loading="lazy"
                class="rounded-full"
              />
            </div>

            <div class="mt-4 text-center">
              <h3 class="text-sm text-gray-500 dark:text-gray-300">
                {" "}
                {item.name}
              </h3>
            </div>
          </div>
        ))
      }
    </div>

    <div class="mx-auto max-w-xl text-center mt-14">
      <p class="text-lg leading-relaxed text-gray-600 dark:text-gray-400">
        Soy una persona multidiciplinaria viajando por el mundo. Este proyecto es testimonio de mis reflexiones
        <br>Contador actual: 11 Paises 
      </p>
      <div class="mt-8">
        <Link href="/contact" style="primary">Escribeme</Link>
      </div>
    </div>
  </Container>
</Layout>
